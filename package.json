{
  "name": "lava-payment",
  "version": "0.0.1",
  "private": true,
  "description": "Web3 payments app for Plasma blockchain",
  "packageManager": "pnpm@9.15.4",
  "scripts": {
    "dev": "pnpm --filter web dev",
    "build": "pnpm -r build",
    "clean": "pnpm -r clean && rm -rf node_modules"
  },
  "engines": {
    "node": ">=18",
    "pnpm": ">=8"
  },
  "dependencies": {
    "qrcode.react": "^4.2.0"
  }
}
